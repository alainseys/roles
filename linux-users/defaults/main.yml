---
# defaults file for user-management
# Opermering: debug_enabled: true will show hashed passwords in output
debug_enabled_default: false
debug_update_password: on_create
default_shell: /bin/bash
default_generate_ssh_key_comment: "ansible-generated for {{ item.username }}@{{ ansible_hostname }}"
