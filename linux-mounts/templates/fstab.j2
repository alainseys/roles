# This file was generated by Ansible
# Do not edit manually

{{ ansible_facts.mounts | selectattr('mount', 'in', mounts | map(attribute='mount') | list) | map(attribute='dump') | join('\n') }}

{% for mount_entry in mounts %}
{{ mount_entry.src if mount_entry.src is defined else "" }} {{ mount_entry.mount }} {{ mount_entry.fstype | default('auto') }} {{ mount_entry.opts | default(mount_options[mount_entry.fstype] if mount_entry.fstype in mount_options else 'defaults') }} {{ mount_entry.dump | default(0) }} {{ mount_entry.passno | default(2) }}
{% endfor %}

# End of Ansible-managed mounts
